<?xml version="1.0" encoding="utf-8"?>
<Window
    x:Class="PixHide.WinUI3App.Views.MainWindow"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:PixHide.WinUI3App.Views"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:controls="using:CommunityToolkit.WinUI.Controls"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:layouts="using:PixHide.WinUI3App.Layouts"
    xmlns:models="using:PixHide.WinUI3App.Models"
    xmlns:viewmodels="using:PixHide.WinUI3App.ViewModels"
    xmlns:sys="using:System"
    mc:Ignorable="d"
    Title="PixHide">

    <Window.SystemBackdrop>
        <MicaBackdrop/>
    </Window.SystemBackdrop>

    <Grid>
        <ContentControl
            HorizontalAlignment="Stretch"
            VerticalAlignment="Stretch"
            HorizontalContentAlignment="Stretch"
            VerticalContentAlignment="Stretch"
            Content="{x:Bind MainViewModel.CurrentViewModel, Mode=OneWay}"
            ContentTemplateSelector="{StaticResource VmTemplateSelector}" />
    </Grid>
</Window>